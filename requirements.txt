# DHT11 Sensor Requirements
# Primary library (recommended)
adafruit-circuitpython-dht

# Alternative libraries (backup options)
Adafruit_DHT
RPi.GPIO

# System dependencies (install with apt-get)
# sudo apt-get install libgpiod2